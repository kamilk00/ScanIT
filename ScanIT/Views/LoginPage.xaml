<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ScanIT.LoginPage"
             xmlns:vm="clr-namespace:ScanIT.ViewModels">
    <ContentPage.Content>
        <StackLayout>
            <Label Text="Login to the ScanIT!" FontSize="Title" HorizontalOptions="CenterAndExpand" Margin="0,20,0,10"/>
            <Entry Placeholder="Username" Text="{Binding username}" />
            <Entry Placeholder="Password" IsPassword="True" Text="{Binding password}" />
            <Button Text="Login" Command="{Binding LoginCommand}" HorizontalOptions="Center" Margin="0,20,0,0"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>