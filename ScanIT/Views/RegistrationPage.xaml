<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ScanIT.RegistrationPage">

    <ContentPage.Content>
        <StackLayout>
            <Label Text="Registration Form" FontSize="Title" HorizontalOptions="CenterAndExpand" Margin="0,20,0,10"/>
            <Entry Placeholder="Username" Text="{Binding username}" />
            <Entry Placeholder="Password" IsPassword="True" Text="{Binding password}" />
            <Button Text="Register" Command="{Binding RegisterCommand}" HorizontalOptions="Center" Margin="0,20,0,0"/>
        </StackLayout>
    </ContentPage.Content>

</ContentPage>
